# Pipeline de RAG — PoC NovaTech (Exercício 1.3)
# Stack open-source / gratuita

chromadb>=1.5.0
sentence-transformers>=2.2.0   # embeddings densos (ideal em produção)
scikit-learn>=1.3.0            # usado pelo fallback TF-IDF offline
numpy>=1.24.0

# Observação: no ambiente de execução sandbox o acesso a huggingface.co é
# bloqueado, então o modelo all-MiniLM-L6-v2 não baixa e o pipeline usa
# automaticamente o fallback TF-IDF (scikit-learn). Em um ambiente com acesso à
# internet, o sentence-transformers carrega normalmente e o backend passa a ser
# semântico denso, sem mudar nenhuma outra parte do código.
